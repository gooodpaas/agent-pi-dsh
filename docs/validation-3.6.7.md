# 3.6.7 升级验证

验证使用现有发行分支、新官方 DSH 0.1.6-alpha.1 固定提交和隔离用户目录。未修改已安装应用或读取账号凭据；模型调用由本地脚本模拟，未进行真实付费模型验收。

- 官方 clean/build 完成，内核源码干净；版本与锁定提交检查通过。
- 脚本回归共 264 项。首轮失败来自新增迁移文件未加入测试夹具及旧版本断言，修订后 15 项失败用例全部通过，其余 249 项首轮通过。
- 宿主回归 257 项首轮通过；Codex 桥接文件须启用 TypeScript transform，补跑其 6 项全部通过。
- 图表零值、负值边界与 UTF-8 目录回填 3 项 Python 回归通过。
- 隔离启动真实 DSH：Office XLSX/DOCX/PPTX 导入、工作树内容、Viewer JS/CSS 与实际界面通过；本地 CAD 渲染通过。
- 普通对话默认无投标项目卡片、知识库为空；显式选择仅作用于该对话，新对话恢复为空。
- 专业深度开关不发送消息、保持当前对话；真实输入后研判、编辑后继续、刷新恢复、关闭模式及精简过程通过。
- 模板由用户编辑保存，不自动选用；手动选择后可查看，并保留至同一对话重开。
- 普通模式下实际执行原生 `skill` 工具加载 huashu-report，未启用专业深度；随后原生交付、文件栏与预览继续工作。
- 原生 Agent Teams：创建成员、查看 roster、创建和刷新共享任务、导航到成员对话通过。未将独立 Codex 执行宣称为可续跑 Team 成员。
- 压缩和明文历史修复测试保留所有事件顺序、原始备份及同一对话状态；重复执行稳定，未完成日志不改写，其他未知事件不放行。新内核原生持久化读取器成功读回修复后的三事件测试日志。
- 一页合成 PDF 实测中文/空格路径、全负数及零值图表、UTF-8 目录修改和几何检查，已渲染成图逐页查看。使用隔离 Python 依赖和现有 Chrome 的测试启动覆盖；文本由 pypdf 检查。未运行缺少 pdftotext 的上游完整机械自检，也不据此宣称所有终端具备完整 PDF 依赖。

安装包和公开资产仍须通过既有完整运行时、原生模块、Office 完整性、CAD 对应源码与构建凭据检查；本文件的源码验收不替代安装包验收。
